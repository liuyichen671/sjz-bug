# 关于“三精准+/云炼营”小程序平台安全漏洞的补充反馈报告（二期）

**反馈背景：**
在之前提交了首份漏洞反馈报告后，出于对全校学生数据安全的担忧，对系统的其他核心业务模块（如打卡、请假、健康档案、报表导出等）进行了进一步的安全性验证。结果发现，除了先前报告的体测档案越权和防篡改缺失外，系统在**学生病假记录（病因/体温）**、**锻炼视频文件存储**、**管理员报表导出**以及**重置他人密码**等更深层面的业务接口上，同样存在极其严重的越权（IDOR）和无鉴权漏洞。

为了彻底消除安全隐患，现将第二批新发现的高危漏洞整理如下，恳请一并转交技术方进行紧急修复。

---

## 补充漏洞总览表

| 漏洞风险点 | 危险等级 | 核心危害说明 |
| :--- | :---: | :--- |
| **任意重置他人密码漏洞** | 🔴 高危 | 仅凭学号即可恶意重置全网任意学生的账号密码 |
| **学生锻炼打卡视频链接未授权访问** | 🔴 高危 | 获取的打卡视频 OSS 链接无访问控制，外部网络可直接播放和下载，存在人脸及家庭隐私泄露风险 |
| **教务管理员 Excel 报表导出接口裸奔** | 🔴 高危 | 无需登录即可触发后端生成全校/全年级打卡统计报表，并获取下载链接 |
| **跨班级/跨区县统计数据越权查看** | 🔴 高危 | 仅凭改变参数，即可查看全区县（如西城区）或全年级的汇总打卡时长和参与率 |
| **学生病假及健康记录越权查看** | 🟡 中危 | 遍历 `classId` 即可获取各班级学生的请假病因（如发烧38度） |
| **AI 运动处方与排名泄露** | 🟡 中危 | 越权获取学生的体质弱项分析（如核心力量差）及在班级、年级的精准排名 |
| **解绑接口越权及数据库逻辑死锁** | 🔴 高危 | 越权破坏他人账号绑定关系，并可通过触发软删除唯一索引冲突，制造大面积数据库 500 异常崩溃 |

---

## 漏洞一：任意重置他人密码漏洞（破坏性攻击）

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    系统提供了一个用于重置学生密码的接口（`/student/resetPassword`），但该接口**完全没有进行任何身份验证或越权校验**。攻击者只需要知道受害者的 `studentId`（可以轻易通过前述的 IDOR 漏洞批量获取），就能在无需原密码、无需手机号验证码的情况下直接将其密码重置。
*   **详细复现：**
    向接口发送请求（注意参数名拼写错误，为系统原生设定）：
    `https://wx.jstzsjz.cn/student/resetPassword?studnetId=[目标学生ID]`
    即可直接将对应学生的密码重置，导致受害学生无法正常登录系统。
*   **影响危害：**
    攻击者可以轻易写个循环脚本，瞬间重置全校乃至全北京市所有学生的密码，造成灾难性的拒绝服务（DoS）和业务停摆，后果极其严重。

## 漏洞二：学生锻炼打卡视频存储链接公网直接可访问

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    系统在处理学生每日打卡视频时，视频文件被直接上传至阿里云 OSS（`osswx.jstzsjz.cn`），且 OSS 存储桶配置为**公共读**。同时，获取打卡记录的接口（`/student/getSignList`）也存在越权，允许任何人通过 `studentId` 获取他人的打卡记录。
*   **详细复现：**
    1. 通过越权接口获取学生的视频地址，如：`https://osswx.jstzsjz.cn/videos/weixin/student/daka/2026-07-13/52739803-2026-07-13-1.mp4`
    2. 将该链接复制到任意浏览器或播放器，**无需任何系统登录态（Token/Cookie）即可直接播放和下载**。
*   **影响危害：**
    打卡视频通常在学生家中拍摄，包含锻炼学生的人脸特征和家庭环境信息。若接口被恶意遍历，可能导致视频被批量下载。建议对 OSS 资源实施鉴权（如使用私有 Bucket + 临时 STS Token 签名 URL）。

## 漏洞三：后台管理员报表导出功能裸奔 —— 核心运营数据泄露

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    通常只有学校教务管理员在后台才能使用的“导出 Excel 报表”功能，其底层接口被直接暴露在公网，且无任何身份鉴权。
*   **详细复现：**
    1. 访问接口：`https://wx.jstzsjz.cn/school/downLoadYunCampResult?type=2&id=[年级ID]&startDate=2026-07-01&endDate=2026-07-14`
    2. 系统会直接响应 HTTP 200，并返回一个已经生成好的 Excel 报表下载链接（例如：`https://osswx.jstzsjz.cn/excel/班级云炼营-2026-07-14-xxx.xlsx`）。
    3. 同理，打卡详情导出接口 `/school/downLoadClockResult` 也存在完全相同的问题。
*   **影响危害：**
    攻击者无需逐个抓取学生数据，利用该接口即可直接下载整个年级或学校的批量汇总 Excel。

## 漏洞四：跨域数据维度越权 —— 获取全区县宏观统计数据

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    云炼营汇总接口（`/school/getYunCampResult`）以及打卡统计接口（`/school/getClockResult`），不仅对请求者身份不加校验，而且完全不对请求的范围（type）做限制。
*   **详细复现：**
    *   通过构造 `type=2&id=[年级ID]`，可以拉取全年级所有班级的横向对比数据（如全班打卡率、人均时长）。
    *   通过构造 `type=4&id=[行政区划代码，如11216西城区]`，甚至可以拉取整个区县的宏观汇总数据。
*   **影响危害：**
    底层的越权导致业务逻辑完全穿透，普通学生账号可以窃取校长级别乃至区教委级别的统计宏观数据。

## 漏洞五：病假记录接口越权（IDOR）

*   **危险级别：** 🟡 中危
*   **漏洞描述：**
    教师端的查看请假列表接口（`/teacher/leaveStudentList`）缺乏最基本的水平权限校验。该接口理应只允许班主任查看本班请假记录，但实际只需修改 URL 中的 `classId` 参数，即可跨越学校和班级限制，查看全网任意班级的请假记录。
*   **详细复现：**
    访问接口：`https://wx.jstzsjz.cn/teacher/leaveStudentList?channel=1&parentId=任意值&classId=[目标班级ID]&date=[日期]`
    返回的 JSON 数据中，明文包含了学生的**真实姓名、学号、请假时间，以及具体病因（如“生病了，发烧38℃请假一天”）**。
*   **影响危害：**
    黑客可编写脚本遍历 `classId`，在短时间内脱库全平台所有中小学生的每日健康状况和病假事由。鉴于目前该数据维度较小，评级为中危。

## 漏洞六：深度个人体质画像及排名泄露

*   **危险级别：** 🟡 中危
*   **漏洞描述：**
    系统在提供深度分析服务时，接口同样存在越权遍历问题。
*   **详细复现：**
    *   通过 `/student/v3/piByStudentId` 接口，可越权查看任意学生的“AI 运动处方”，明文暴露学生的体能短板（如“核心力量极差”）。
    *   通过 `/student/querystudentscoreranking` 接口，可精准越权获取任意学生在全班、全年级的体测分数排名百分比。

## 漏洞七：解绑接口存在逻辑死锁与潜在拒绝服务攻击（DoS）

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    账号解绑接口（`/student/unBind`）在数据库层面存在严重的逻辑设计缺陷。系统采用“软删除”（将 `is_enabled` 置为 0）来处理解绑记录，但却将 `is_enabled` 字段加入了 `t_parent_student_combine` 表的唯一联合索引（`uk_student_parent`）中。
*   **详细复现：**
    1. 访问接口 `https://wx.jstzsjz.cn/student/unBind?studentNum=[学号]&parentId=[家长ID]` 进行首次解绑，成功。
    2. 重新进行账号绑定，生成一条新的合法记录。
    3. 再次尝试解绑，由于第一次解绑的“软删除遗留数据”与新产生的数据在修改状态后触碰了联合索引的唯一性限制，系统直接抛出 HTTP 500 内部服务器错误，提示 `SQLIntegrityConstraintViolationException` (Duplicate entry)。
*   **影响危害：**
    一旦账号解绑过一次，将永远无法进行第二次解绑。更为严重的是，该接口参数全为明文且缺乏身份校验，攻击者不仅能批量越权解绑全校学生的账号，还能利用该底层缺陷故意制造大量脏数据和严重的数据库异常错误，存在引发拒绝服务攻击（DoS）瘫痪数据库的风险。

---

## 补充修复建议（供技术方参考）

除了此前建议的“全面强制接入身份认证（Token/Session）”之外，针对本次新发现的漏洞，强烈建议补充以下安全措施：

1.  **高危写接口全面封闭：** 像密码重置这种具备破坏性的接口必须强制要求旧密码校验或短信验证码，并且必须采用 POST 请求防止跨站请求伪造（CSRF）。
2.  **OSS 存储桶权限收严：** 立即将存放学生打卡视频和图片的 OSS Bucket 权限从“公共读”修改为“私有”。所有前端请求媒体资源必须向后端申请带有过期时间（如 15 分钟）的签名 URL（Presigned URL）。
3.  **数据隔离与鉴权（Data-level Authorization）：** 后端在处理带有 `classId`、`schoolId` 或 `type=2/3/4` 的请求时，不能仅判断用户是否登录，**必须判断当前登录用户的角色（Role）及其管辖范围**。例如：班主任角色只能查询绑定在自己名下的 `classId`，绝不能允许查询隔壁班的数据。
4.  **报表导出接口封堵：** 报表生成及 Excel 下载这类消耗大量服务器资源的接口，必须严禁在无鉴权的外部网关暴露，甚至应限制只有在校园内网或管理员 VPN 下才能调用。
