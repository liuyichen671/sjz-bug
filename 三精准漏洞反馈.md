# 关于“三精准+/云炼营”小程序平台安全漏洞的反馈报告

**反馈背景：**
在使用本校要求的“云炼营/学生体质健康”相关小程序（域名：`wx.jstzsjz.cn`）期间，偶然发现该系统在底层接口设计上存在极其严重的安全隐患。这些漏洞不仅导致学生可以轻易绕过系统限制和作弊，更严重的是，**极易导致全北京市（覆盖多所中小学）海量学生的真实姓名、学号、班级、身高等隐私数据遭到大面积泄露或恶意篡改。**

为了防止系统遭到恶意攻击或导致重大数据安全事故，现将发现的主要漏洞整理如下，恳请老师协助转交至平台的技术开发方或相关负责人进行紧急修复。

---

## 漏洞总览表

| 漏洞风险点 | 危险等级 | 状态 | 核心危害说明 |
| :--- | :---: | :---: | :--- |
| **无身份认证机制** | 🔴 高危 | 已确认 | 核心接口无 Token/Session 校验，任何人可任意调用 |
| **越权访问（IDOR）查看隐私** | 🔴 高危 | 已确认 | 仅凭连续的 studentId 即可遍历并脱库北京市上百万学生隐私 |
| **伪造自己的锻炼时长** | 🔴 高危 | 已确认 | 任意伪造虚假数据上报 |
| **篡改他人锻炼时长** | 🔴 高危 | 已确认 | 跨用户操作，恶意篡改或破坏其他同学记录 |
| **接受负数/超大时长参数** | 🔴 高危 | 已确认 | 后端无数据边界校验，导致系统数据结构异常 |
| **Swagger API 文档裸奔** | 🔴 高危 | 已确认 | 生产环境暴露所有内部接口，相当于公网开放系统“后门” |
| **GET 请求修改数据** | 🟡 中危 | 已确认 | 违背开发规范，敏感操作极易被诱导点击利用 |
| **跨班级排名查看** | 🟡 中危 | 已确认 | 绕过权限查看其他班级汇总数据 |
| **视频解锁绕过** | 🟡 中危 | 已确认 | 前端判断缺陷，提前下发全周期敏感数据 |

---

## 漏洞一：严重的越权访问漏洞（IDOR）—— 百万级学生隐私泄露风险

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    系统在获取学生敏感数据（如体质雷达图、历年身高体重记录、体测成绩等）时，完全没有任何身份认证鉴权机制。接口仅仅依靠 URL 中的 `studentId` 参数来返回数据。
*   **详细复现方法：**
    1. 打开任意浏览器（无需登录）。
    2. 访问以下接口地址，并随机修改末尾的 `studentId` 数字（如 52396384）：
       `https://wx.jstzsjz.cn/student/getStudentRadar?studentId=52737725`
    3. 即可在页面中直接看到完整的 JSON 数据返回，明文包含了**真实姓名、学号、班级、学校，以及历次体测详情**。
*   **影响危害：**
    经测试，该平台的 `studentId` 是连续的。任何人只需编写脚本循环请求，**几小时内即可将系统中上百万名北京市中小学生的隐私数据全部窃取（脱库）。**

## 漏洞二：接口缺乏校验与防篡改机制 —— 锻炼时长随意伪造与破坏

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    上报锻炼时长的接口完全信任前端传来的数值，不对上限和下限做任何业务逻辑判断。且系统接收新时长后，只与历史记录取最大值（`Math.max`）。
*   **详细复现方法：**
    1. 打开任意浏览器访问以下链接（尝试将某学生时长设置为极大值，例如 999999 秒）：
       `https://wx.jstzsjz.cn/yuncamp/setduration?studentId=52737724&campInfoId=823&duration=999999`
    2. 页面返回 `{"code":0}`。再次查看该学生的云炼营记录，会发现其总时长已被永久锁定在 **277.8小时**，且永远无法改回正常的小数值。
*   **影响危害：**
    任何人可以跨用户篡改他人数据，甚至传入负数破坏数据结构。（本人账号 `52737724` 已因误触此漏洞导致时长卡死，恳请管理员后台协助重置为 1216 秒）。

## 漏洞三：前端视频解锁控制缺陷 —— 提前获取全部课程视频

*   **危险级别：** 🟡 中危
*   **漏洞描述：**
    获取云炼营列表时（`/yuncamp/nowyuncamp`），后端一次性下发了整个营期（共48天）尚未解锁的所有视频真实地址，仅靠前端页面时间隐藏。
*   **影响危害：**
    用户可通过抓包轻易提取所有隐藏的 mp4 链接，暴露了极不严谨的 API 接口设计规范。

## 漏洞四：Swagger API 文档公网裸奔与教师端权限失控

*   **危险级别：** 🔴 高危
*   **漏洞描述：**
    系统在生产环境中未关闭 Swagger UI 接口文档（`https://wx.jstzsjz.cn/swagger-ui.html`）。通过该文档，可以清晰地看到系统暴露了多达 **160 余个** 接口。
*   **影响危害（教师端接口失控）：**
    由于系统普遍缺乏鉴权，文档中暴露的近 70 个 **教师管理端高危接口** 可被任何人直接调用，产生毁灭性后果。部分极度危险接口举例：
    *   🔴 `/teacher/createStudent`（创建学生）：可以凭空捏造虚假学生数据。
    *   🔴 `/teacher/deleteStudent`（删除学生）：可以任意删掉库中的真实学生数据。
    *   🔴 `/teacher/updateStudent`（修改学生信息）：可以篡改任意学生资料。
    *   🔴 `/teacher/updatePassWord`（修改教师登录密码）：可以恶意劫持合法教师账号。
    *   🔴 `/teacher/update`（修改教师信息）：可以篡改教师资料。
    *   🔴 `/teacher/regist`（教师注册）：可能允许攻击者自己注册成为管理员老师！
    *   🟡 `/teacher/getStudentList` 与 `/teacher/classList`：方便攻击者批量枚举所有班级和拖取全量数据。

---

## 修复建议（供技术方参考）

鉴于漏洞的严重性和易利用性，建议开发方立即采取以下应急和修复措施：

1.  **紧急下线或屏蔽调试端点：** 立即在网关层或代码层封禁 `/swagger-ui.html`、`/v2/api-docs` 及所有的 Spring Boot Actuator 端点（`/actuator/*`）。
2.  **全面强制接入身份认证鉴权（Authentication & Authorization）：**
    *   废弃仅凭 `studentId` 明文参数获取数据的设计。
    *   全面引入 Token（如 JWT）或 Session 机制，并在后端严格校验当前登录用户的越权操作。
    *   **严格隔离教师端与学生端权限，拦截所有未经授权的 `/teacher/*` 请求。**
3.  **增加业务逻辑的参数校验：** 对于时长等数值型输入，后端必须增加合理的边界校验（不能超过合理时间，不能为负数）。
4.  **改用 POST 动词进行数据修改：** 涉及数据变更的操作（如 `setduration`）必须使用 POST 请求，并通过请求体传递参数。
5.  **严格遵循“最小权限返回”原则：** 避免未解锁视频链接等敏感数据提前推送到前端。

---
*注：以上漏洞均仅通过合法途径进行有限度的非破坏性只读抽样探测，整理此报告旨在帮助学校和平台方尽早排查隐患，保护广大学生的隐私安全。*
